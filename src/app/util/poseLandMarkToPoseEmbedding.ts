import { Pose as BlazePosePose } from '@tensorflow-models/pose-detection';
import * as tf from '@tensorflow/tfjs';

function getCenterPoint(landmarks: tf.Tensor, leftBodyPart: BodyPart, rightBodyPart: BodyPart): tf.Tensor {
  tf.gather();

  return center;
}

export function poseLandMarkToPoseEmbedding(pose: BlazePosePose) {}
